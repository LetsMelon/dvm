[package]
name = "dvm"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_plain = "1.0.2"

[profile.profiling]
inherits = "release"
debug = true
